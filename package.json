{
  "name": "prettier-plugin-prisma",
  "version": "2.28.0",
  "description": "Prettier plugin for Prisma files",
  "keywords": [
    "prettier-plugin",
    "prisma"
  ],
  "repository": "git@github.com:umidbekk/prettier-plugin-prisma.git",
  "license": "MIT",
  "author": "Umidbek Karimov",
  "main": "lib/plugin.js",
  "scripts": {
    "build": "node scripts/build.js",
    "lint": "prettier . --check",
    "prerelease": "npm run lint && npx tsc && npm run build",
    "release": "np --no-2fa",
    "pretdd": "npm run build",
    "tdd": "jest --watch",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^26.0.24",
    "@types/prettier": "^2.3.2",
    "execa": "^5.1.1",
    "jest": "^27.0.6",
    "np": "^7.5.0",
    "prettier": "^2.3.2",
    "prettier-plugin-packagejson": "^2.2.11",
    "typescript": "^4.3.5"
  },
  "peerDependencies": {
    "prettier": ">=2"
  },
  "engines": {
    "node": ">=12"
  }
}
